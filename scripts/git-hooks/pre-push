#!/bin/sh
set -eu

ROOT_DIR="$(git rev-parse --show-toplevel)"

echo "[pre-push] Running fast gate (lint + typecheck). Skip with 'git push --no-verify'."
pnpm --dir "$ROOT_DIR" lint
pnpm --dir "$ROOT_DIR" typecheck
